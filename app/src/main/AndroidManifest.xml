<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.radiohacks.frinmean">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StartActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".ChatActivity"
            android:label="@string/title_activity_chat" />
        <activity
            android:name=".SignUpActivity"
            android:label="@string/title_activity_sign_up" />
        <activity
            android:name=".SingleChatActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.DarkActionBar"
            android:label="@string/title_activity_single_chat" />
        <activity android:name="net.rdrei.android.dirchooser.DirectoryChooserActivity" />

        <service
            android:name=".service.MeBaService"
            android:enabled="true"
            android:exported="false"></service>
        <receiver
            android:name=".StartActivity$AuthenticateStateReceiver"
            android:exported="false"
            android:enabled="true" />
        <receiver android:name=".SingleChatActivity$SingleChatReceiver"
            android:enabled="true"
            android:exported="false" />
        <receiver android:name=".ChatActivity$ListChatStateReceiver"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>
